<!-- This was a page of its own; the site is one scroll now. Kept as a stop so
     the old link still lands where the content went. A meta refresh rather
     than a load-time redirect: the site prerenders to static files, and this
     is the only redirect a static host serves without a rule of its own. -->
<svelte:head>
	<title>Contact — Joshua Smith</title>
	<meta http-equiv="refresh" content="0; url=/#contact" />
</svelte:head>

<a href="/#contact">Continue to Contact</a>
